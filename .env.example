# DashScope API Key，用于调用达摩盘文本向量服务
DASHSCOPE_API_KEY=your_dashscope_key_here

# 可选：指定向量数据库保存路径（需为纯 ASCII 路径，避免 FAISS 在中文目录写入失败）
# VECTOR_DATA_DIR=C:\faiss_data

# 可选：Redis 连接信息（默认 localhost:6379，无密码）
# REDIS_URL=redis://:password@localhost:6379/0
